// @vitest-environment jsdom
import{beforeEach,describe,expect,it,vi}from'vitest';import{flushPromises,mount}from'@vue/test-utils';import ElementPlus from'element-plus';import Manager from'./Manager.vue';import{listEndpoints,listSubscriptions,createSubscription}from'../../api/integration';import{listObjectTypes}from'../../api/mdm';
vi.mock('../../api/integration',()=>({listEndpoints:vi.fn(),listSubscriptions:vi.fn(),createEndpoint:vi.fn(),createSubscription:vi.fn()}));vi.mock('../../api/mdm',()=>({listObjectTypes:vi.fn()}));
describe('integration subscriptions',()=>{beforeEach(()=>{vi.clearAllMocks();listEndpoints.mockResolvedValue({data:[{id:1,code:'ERP'}]});listSubscriptions.mockResolvedValue({data:[]});listObjectTypes.mockResolvedValue({data:[{id:2,code:'person',name:'Person'}]});createSubscription.mockResolvedValue({data:{id:3}})});
it('loads choices and creates a scoped subscription payload',async()=>{const w=mount(Manager,{global:{plugins:[ElementPlus]}});await flushPromises();await w.get('[data-test="subscription-endpoint"]').trigger('click');w.vm.subscription.endpoint_id=1;w.vm.subscription.object_type_id=2;w.vm.subscription.event_type='RECORD_CHANGED';await w.get('[data-test="create-subscription"]').trigger('click');await flushPromises();expect(listObjectTypes).toHaveBeenCalled();expect(createSubscription).toHaveBeenCalledWith({endpoint_id:1,object_type_id:2,event_type:'RECORD_CHANGED'});expect(listSubscriptions).toHaveBeenCalledTimes(2);});
});
